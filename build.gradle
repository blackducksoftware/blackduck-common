buildscript {
    apply from: 'https://raw.githubusercontent.com/blackducksoftware/integration-resources/master/gradle_common/buildscript-repositories.gradle', to: buildscript
    ////////// START BUILDSCRIPT DEPENDENCY //////////
dependencies { classpath "com.synopsys.integration:common-gradle-plugin:1.7.0" }

////////// END BUILDSCRIPT DEPENDENCY //////////
}

project.ext.moduleName = 'com.synopsys.integration.blackduck-common'
project.ext.javaUseAutoModuleName = 'true'
project.ext.junitShowStandardStreams = 'true'

version = '50.0.3'
description = 'A library for using various capabilities of Black Duck, notably the REST API and signature scanning.'

apply plugin: 'com.synopsys.integration.library'

final def versionFile = new File("${projectDir}/src/main/resources/com/synopsys/integration/blackduck/version.txt")
versionFile.mkdirs()
versionFile.delete()
versionFile << version

dependencies {
    api 'com.synopsys.integration:blackduck-common-api:2019.12.0.18'
    api 'com.synopsys.integration:integration-reporting:2.0.0'
    api 'com.synopsys.integration:integration-bdio:22.0.0'
    api 'com.synopsys.integration:phone-home-client:4.0.0'

    api 'com.blackducksoftware.bdio:bdio2:3.0.0-beta.47'

    testImplementation 'org.hamcrest:hamcrest-core:1.3'
    testImplementation 'org.mockito:mockito-core:2.18.3'
    testImplementation 'com.squareup.okhttp3:mockwebserver:4.7.2'
    testImplementation 'org.junit.jupiter:junit-jupiter-params:5.3.1'

    testRuntime 'org.slf4j:slf4j-simple:1.7.30'
}
